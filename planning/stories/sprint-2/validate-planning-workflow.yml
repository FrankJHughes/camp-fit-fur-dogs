title: "Validate Planning Workflow"
description: |
  Add a CI gate that validates planning artifact integrity — ensuring every story YAML
  has required fields, source.productFile references exist, and sprint manifests
  reference only existing story files.
tasks:
  - "Create a validation script that checks YAML schema compliance"
  - "Add file-existence checks for source.productFile references"
  - "Validate sprint manifest entries reference existing story files"
  - "Wire validation into GitHub Actions CI workflow"
acceptance:
  - "CI workflow validates all planning YAMLs have required fields"
  - "Every YAML with source.productFile is checked for file existence"
  - "Sprint manifest entries reference existing story files"
  - "Validation failures block merge with clear error messages"
epic: "Backlog Grooming Experience"
labels:
  - infra
  - ci
  - governance
points: 3
priority: P1
branch: "chore/validate-planning-workflow"
source:
  productFile: "/product/stories/infra/validate-planning-workflow.md"

