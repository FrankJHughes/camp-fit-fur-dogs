namespace CampFitFurDogs.Domain;

/// <summary>
/// Serves as a marker type for the <c>CampFitFurDogs.Domain</c> assembly.
/// <para>
/// This type is intentionally empty and exists solely to provide a stable
/// reference point for assembly‑based discovery operations, such as scanning
/// for domain types, value objects, aggregates, and domain exceptions.
/// </para>
/// <para>
/// Usage typically involves passing <c>typeof(AssemblyMarker).Assembly</c>
/// to infrastructure or application‑layer registration methods that need to
/// locate domain types without relying on string‑based assembly names.
/// </para>
/// </summary>
public sealed class AssemblyMarker { }
